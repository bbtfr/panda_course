<!DOCTYPE html>
<html>
<head>
  <meta http-equiv="X-UA-Compatible" content="IE=edge">

  <title>Panda Course 2015</title>

  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>

  <%= csrf_meta_tags %>

  <!-- HTML5 shim, for IE6-8 support of HTML5 elements -->
  <!--[if lt IE 9]>
    <%= javascript_include_tag  "html5shiv.min" %>
    <%= javascript_include_tag  "respond.min" %>
  <![endif]-->
</head>
<body>

  <header class="masthead <%= active?("index") ? "jumbotron" : "well"  %>">
    <div class="container">
      <h2><%= t "subtitle", scope: :layout %></h2>
      <h1><%= t "title", scope: :layout %></h1>
      <p>
        <%= t "location", scope: :layout %>
        &nbsp;&nbsp;
        <%= t "time", scope: :layout %>
      </p>
    </div>
    <div class="right-top">
      <div class="languages">
        Language:
        <a <%= raw locale?(:en) ? "class=\"active\"" : "href=\"#{english_path}\"" %>>English</a>
        |
        <a <%= raw locale?(:"zh-CN") ? "class=\"active\"" : "href=\"#{chinese_path}\"" %>>简体中文</a>
      </div>
      <div class="weixin hidden-xs">
        <%= t(:wechat, scope: :layout) %><br>
        <%= image_tag "images/QR.png", width: 75 %>

        <%= image_tag "images/QR.png", class: "qrcode bounceIn animated", width: 300 %>
      </div>
    </div>
  </header>

  <div class="container">
    <nav class="navbar navbar-default" role="navigation">
      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="<%= page_path(id: :index) %>">PANDA 2015</a>
      </div>

      <div class="collapse navbar-collapse" id="navbar-collapse">
        <ul class="nav navbar-nav">
          <% t("navbar", scope: :layout).each do |key, value| %>
            <li <%= raw "class=\"active\"" if active? key %>><%= link_to value, page_path(id: key) %></li>
          <% end %>
          <li><a href="/2014/index.html">PANDA2014</a></li>
        </ul>
      </div>
    </nav>

    <% flash.each do |key, value| %>
      <div class="alert alert-<%= key %> alert-dismissible">
        <button type="button" class="close" data-dismiss="alert"><span>&times;</span><span class="sr-only">Close</span></button>
        <%= value %>
      </div>
    <% end %>

    <%= yield %>

  </div>

  <footer class="footer">
    <div class="container">
      <p class="text-muted">Designed and built with <a href="http://getbootstrap.com/" target="_blank">Twitter Bootstrap</a>.</p>
    </div>
  </footer>

</body>
</html>
